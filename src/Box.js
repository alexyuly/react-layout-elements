import React from 'react'

const Box = ({
  children,
}) => (
  <div>
    {children}
  </div>
)

export default Box

// A Box element stretches to fit all of the available space allocated by its parent's layout.
